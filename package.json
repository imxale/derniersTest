{
  "scripts": {
    "test": "jest --watchAll --verbose --coverage",
    "start": "node app.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "faker-br": "^0.1.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
